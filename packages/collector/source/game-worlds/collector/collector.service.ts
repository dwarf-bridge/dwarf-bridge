import { Injectable } from '@nestjs/common';

@Injectable()
export class Collector {
  async collect() {
    try {
      /**
       * get worlds
       */
    } catch (err) {}
  }
  //   async run(options: { world_name: string }) {}
}
